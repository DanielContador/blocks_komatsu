query local_dlservices_items($searchTerm: String, $spage: Int){
    local_dlservices_items(searchTerm: $searchTerm, spage: $spage) {
        items{
            id
            category
            shortname
            fullname
            link
            imageUrl
            duration
            top
            isNew
            itemType
        }
    }
}

